---
import V2bLayout from '../../../layouts/V2bLayout.astro';
import Icon from '../../../components/v2/Icon.astro';

const cibles = [
  { icon: 'hard-hat',    metier: 'Artisans BTP',                pitch: 'Vos chantiers, vos devis, votre tréso — sur un seul écran.' },
  { icon: 'building',    metier: 'Architectes',                 pitch: 'Vos affaires pilotées, votre focus création préservé.' },
  { icon: 'building-2',  metier: 'Promoteurs immobiliers',      pitch: 'Du foncier à la livraison, vos opérations en temps réel.' },
  { icon: 'home',        metier: 'Agences immobilières',        pitch: 'Mandats, prospects, commercialisations — un seul flux.' },
  { icon: 'mail',        metier: 'Syndics & gestion locative',  pitch: 'Cinq cents mails triés, courriers générés, AG suivies.' },
  { icon: 'sparkles',    metier: 'Un autre métier ?',           pitch: '9·58 répond à toute TPE qui a perdu la vue d\'ensemble.' },
];

const proofs = [
  { value: '+500', label: 'personnes formées depuis 2022' },
  { value: '6',    label: 'démos sectorielles déployées' },
  { value: '100%', label: 'européen — souverain par défaut' },
  { value: '48h',  label: 'pour vous répondre' },
];

const temoignages = [
  {
    secteur: 'Promotion foncière',
    quote: 'On a passé deux jours à expliquer comment on travaille. Trois mois plus tard, je ne reviendrai pas à mon Excel.',
    result: 'Suivi multi-opérations consolidé',
  },
  {
    secteur: 'Syndic de copropriété',
    quote: 'Cinq cents mails par jour. Avant, je rentrais chez moi à 21 h. Aujourd\'hui à 18 h 30, et mieux fait.',
    result: '2,5 h gagnées par jour',
  },
  {
    secteur: 'Gestion de patrimoine',
    quote: 'Les analyses sont devenues des supports clients à la charte du cabinet. En un clic, je présente.',
    result: 'Présentations en 1 clic',
  },
];
---

<V2bLayout
  title="On installe l'IA dans votre entreprise"
  description="60 % de votre croissance dort déjà dans vos données. 9·58 construit l'outil sur-mesure qui les fait parler. Pour artisans, architectes, promoteurs, agences et syndics. Vannes & Paris."
>
  <!-- =========================================
       HEADER
       ========================================= -->
  <header class="site-header" role="banner">
    <div class="container header-inner">
      <a href="/" class="brand" aria-label="9·58, retour à l'accueil">
        <span class="brand-mark">9·58</span>
        <span class="brand-tag">Neuf Cinquante Huit</span>
      </a>
      <nav class="nav nav-desktop" aria-label="Navigation principale">
        <ul>
          <li><a href="#pourquoi-maintenant">Pourquoi maintenant</a></li>
          <li><a href="#vos-donnees">Vos données</a></li>
          <li><a href="#construit">Ce qu'on construit</a></li>
          <li><a href="#methode">Méthode</a></li>
          <li><a href="#preuve">Réalisations</a></li>
        </ul>
      </nav>
      <a href="#contact" class="btn btn--primary btn--sm header-cta">
        Diagnostic
        <Icon name="arrow-right" size={14} class="arrow" />
      </a>
    </div>
  </header>

  <!-- =========================================
       HERO — Pivot B : texte court + démo cockpit live
       ========================================= -->
  <section class="hero" aria-labelledby="hero-title">
    <div class="container hero-grid">

      <!-- Colonne texte (gauche) -->
      <div class="hero-copy">
        <p class="eyebrow reveal">
          9·58 — Bretagne, depuis 2022
        </p>

        <h1 id="hero-title" class="manifesto reveal">
          On installe l'IA<br>
          dans votre entreprise.
        </h1>

        <p class="killer-line reveal">
          Votre entreprise<br>
          dans votre poche.
        </p>

        <div class="hero-ctas reveal">
          <a href="#contact" class="btn btn--primary btn--lg">
            Demander un diagnostic
            <Icon name="arrow-right" size={18} class="arrow" />
          </a>
          <a href="https://demo.958.fr" class="btn btn--ghost btn--lg">
            Voir une démo
            <Icon name="arrow-up-right" size={18} class="arrow" />
          </a>
        </div>

        <ul class="hero-assurances reveal">
          <li>Réponse sous 48 h</li>
          <li>NDA</li>
          <li>Premier échange offert</li>
        </ul>
      </div>

      <!-- Colonne démo cockpit (droite) -->
      <div class="hero-demo reveal" aria-label="Aperçu de l'écran 9·58">
        <div class="cockpit">
          <header class="cockpit-bar">
            <span class="cockpit-brand">9·58</span>
            <span class="cockpit-cabinet">Cabinet Martin · Vannes</span>
            <span class="cockpit-version">v1.7 · sur-mesure</span>
            <span class="cockpit-time" id="cockpit-time">08:47</span>
          </header>

          <div class="cockpit-grid">
            <!-- Card Tréso -->
            <article class="cockpit-card cockpit-card--treso">
              <div class="cc-head">
                <span class="cc-label">Tréso prévue 90 j</span>
                <span class="cc-dot cc-dot--warn" aria-hidden="true"></span>
              </div>
              <div class="cc-value">
                +<span class="treso-num">28 540</span> €
              </div>
              <div class="cc-spark" aria-hidden="true">
                <svg viewBox="0 0 100 30" preserveAspectRatio="none">
                  <polyline points="0,18 12,15 24,20 36,12 48,14 60,8 72,16 84,22 96,26"
                            fill="none" stroke="currentColor" stroke-width="1.6"/>
                  <circle cx="96" cy="26" r="2.5" fill="currentColor"/>
                </svg>
              </div>
              <p class="cc-note">↘ -3 200 € prévu semaine 34</p>
            </article>

            <!-- Card Chantiers -->
            <article class="cockpit-card cockpit-card--chantiers">
              <div class="cc-head">
                <span class="cc-label">3 chantiers en cours</span>
              </div>
              <ul class="cc-chantiers">
                <li>
                  <div class="cc-chantier-row">
                    <span class="cc-tag cc-tag--ok">✓</span>
                    <span class="cc-chantier-nom">Klein — gros œuvre</span>
                    <span class="cc-chantier-pct">68 %</span>
                  </div>
                  <div class="cc-progress"><span style="width: 68%; background: var(--vert-mousse);"></span></div>
                </li>
                <li>
                  <div class="cc-chantier-row">
                    <span class="cc-tag cc-tag--warn">!</span>
                    <span class="cc-chantier-nom">Lefèvre — second œuvre</span>
                    <span class="cc-chantier-pct">42 %</span>
                  </div>
                  <div class="cc-progress"><span style="width: 42%; background: var(--warn);"></span></div>
                </li>
                <li>
                  <div class="cc-chantier-row">
                    <span class="cc-tag">·</span>
                    <span class="cc-chantier-nom">Dupuis — finitions</span>
                    <span class="cc-chantier-pct">91 %</span>
                  </div>
                  <div class="cc-progress"><span style="width: 91%; background: var(--cuivre);"></span></div>
                </li>
              </ul>
            </article>

            <!-- Card Mails IA -->
            <article class="cockpit-card cockpit-card--mails">
              <div class="cc-head">
                <span class="cc-label">12 mails lus ce matin</span>
              </div>
              <div class="cc-mail-draft">
                <span class="cc-mail-to">→ Sophie Lefort</span>
                <p class="cc-mail-body">
                  <span class="typing">Bonjour Sophie, suite à notre…</span>
                </p>
                <span class="cc-mail-badge">Brouillon prêt</span>
              </div>
            </article>

            <!-- Card RH / Équipe -->
            <article class="cockpit-card cockpit-card--rh">
              <div class="cc-head">
                <span class="cc-label">Équipe — semaine 22</span>
              </div>
              <ul class="cc-rh">
                <li>
                  <span class="cc-rh-name">Pierre M.</span>
                  <div class="cc-progress"><span style="width: 82%; background: var(--vert-mousse);"></span></div>
                  <span class="cc-rh-pct">82 %</span>
                </li>
                <li>
                  <span class="cc-rh-name">Sophie L.</span>
                  <div class="cc-progress"><span style="width: 91%; background: var(--cuivre);"></span></div>
                  <span class="cc-rh-pct">91 %</span>
                </li>
                <li>
                  <span class="cc-rh-name">Marc D.</span>
                  <div class="cc-progress"><span style="width: 38%; background: var(--ink-3);"></span></div>
                  <span class="cc-rh-pct">38 %</span>
                </li>
              </ul>
              <p class="cc-note cc-note--info">+ 1 congé prévu semaine 24</p>
            </article>

            <!-- Card Pipeline Kanban (full-width) -->
            <article class="cockpit-card cockpit-card--kanban">
              <div class="cc-head">
                <span class="cc-label cc-label--ai">
                  <span class="cc-pulse" aria-hidden="true"></span>
                  Pipeline — auto-trié par l'IA
                </span>
                <span class="cc-kanban-meta">↻ 2 cartes déplacées ce matin</span>
              </div>
              <div class="cc-kanban">
                <div class="cc-kcol">
                  <span class="cc-kcol-h">Devis</span>
                  <span class="cc-kcard">Garage L. — 8 200 €</span>
                  <span class="cc-kcard cc-kcard--moving">Cabinet R. — 4 750 €</span>
                  <span class="cc-kcol-count">3</span>
                </div>
                <div class="cc-kcol">
                  <span class="cc-kcol-h">Accepté</span>
                  <span class="cc-kcard">Klein — 18 400 €</span>
                  <span class="cc-kcol-count">2</span>
                </div>
                <div class="cc-kcol">
                  <span class="cc-kcol-h">Chantier</span>
                  <span class="cc-kcard">Lefèvre — 32 100 €</span>
                  <span class="cc-kcard">Dupuis — 7 800 €</span>
                  <span class="cc-kcol-count">4</span>
                </div>
                <div class="cc-kcol">
                  <span class="cc-kcol-h">Facturé</span>
                  <span class="cc-kcard">Morel — 12 600 €</span>
                  <span class="cc-kcol-count">1</span>
                </div>
              </div>
            </article>

            <!-- Card Suggestion IA (full-width) -->
            <article class="cockpit-card cockpit-card--ai">
              <div class="cc-head">
                <span class="cc-label cc-label--ai">
                  <span class="cc-pulse" aria-hidden="true"></span>
                  Suggestion IA
                </span>
              </div>
              <p class="cc-ai-text">
                Client Klein paie en moyenne <strong>+22 jours</strong> de retard sur les 3 derniers chantiers.
                Demander un acompte de 30 % au prochain devis ?
              </p>
              <div class="cc-ai-actions">
                <button class="cc-ai-btn cc-ai-btn--primary" type="button">Appliquer</button>
                <button class="cc-ai-btn" type="button">Plus tard</button>
              </div>
            </article>
          </div>
        </div>
      </div>

    </div>
  </section>

  <script>
    // Anim subtile : l'heure qui avance + le chiffre tréso qui varie légèrement
    const timeEl = document.getElementById('cockpit-time');
    const tresoEl = document.querySelector('.treso-num');
    if (timeEl) {
      let h = 8, m = 47;
      setInterval(() => {
        m++;
        if (m >= 60) { m = 0; h++; }
        timeEl.textContent = `${String(h).padStart(2,'0')}:${String(m).padStart(2,'0')}`;
      }, 3500);
    }
    if (tresoEl) {
      let val = 28540;
      setInterval(() => {
        val += Math.round((Math.random() - 0.5) * 800);
        tresoEl.textContent = val.toLocaleString('fr-FR');
      }, 4200);
    }
  </script>

  <!-- =========================================
       BANDEAU CLARIF — anti "ça fait CRM"
       ========================================= -->
  <section class="section section--clarif is-tight" aria-label="Précision sur le produit">
    <div class="container is-default">
      <p class="reveal clarif-text">
        Ce que vous venez de voir, c'est l'écran d'un <strong>artisan bâtiment de Bretagne</strong>.
        Construit pour <em>ses</em> devis, <em>ses</em> chantiers, <em>son</em> équipe.
        <strong>Pas un CRM. Pas un logiciel comptable. Pas un outil pour tout le monde.</strong>
        Le vôtre ne lui ressemblera pas — il vous ressemblera, à vous.
      </p>
    </div>
  </section>

  <!-- =========================================
       SECTION 1 — POURQUOI MAINTENANT (courbe d'adoption)
       ========================================= -->
  <section id="pourquoi-maintenant" class="section section--soft" aria-labelledby="adoption-title">
    <div class="container">
      <div class="section-head">
        <p class="eyebrow reveal">Pourquoi maintenant</p>
        <h2 id="adoption-title" class="reveal section-h2">
          Tout le monde parle d'IA.<br>
          <em>Très peu l'installent vraiment.</em>
        </h2>
        <p class="reveal section-sub">
          La courbe d'adoption est lancée. Pas en panique. À votre rythme.
          Mais sachez où vous êtes — et où sont les autres.
        </p>
      </div>

      <!-- Courbe d'adoption SVG (S-curve Rogers) -->
      <div class="adoption-curve reveal">
        <svg viewBox="0 0 1000 380" preserveAspectRatio="xMidYMid meet" class="adoption-svg" role="img" aria-label="Courbe d'adoption de l'IA dans les TPE-PME 2024-2027">

          <!-- Grille discrète -->
          <line x1="60" y1="320" x2="960" y2="320" stroke="currentColor" stroke-opacity="0.12" stroke-dasharray="2 4"/>
          <line x1="60" y1="200" x2="960" y2="200" stroke="currentColor" stroke-opacity="0.06" stroke-dasharray="2 4"/>
          <line x1="60" y1="80"  x2="960" y2="80"  stroke="currentColor" stroke-opacity="0.06" stroke-dasharray="2 4"/>

          <!-- Aire sous la courbe (subtile) -->
          <path d="M 60,320 C 220,318 320,310 420,240 C 520,170 640,90 800,70 C 880,62 940,60 960,60 L 960,320 Z"
                fill="url(#curveGradient)" opacity="0.18"/>
          <defs>
            <linearGradient id="curveGradient" x1="0" y1="0" x2="0" y2="1">
              <stop offset="0%" stop-color="#C75F2D" stop-opacity="0.5"/>
              <stop offset="100%" stop-color="#C75F2D" stop-opacity="0"/>
            </linearGradient>
          </defs>

          <!-- Courbe en S -->
          <path class="adoption-path"
                d="M 60,320 C 220,318 320,310 420,240 C 520,170 640,90 800,70 C 880,62 940,60 960,60"
                fill="none" stroke="#C75F2D" stroke-width="3"
                stroke-linecap="round"/>

          <!-- Dot 1 — 2024 Pionniers -->
          <g transform="translate(160, 316)" class="ap-svg-g">
            <circle r="9" fill="#6B8E54"/>
            <circle r="13" fill="none" stroke="#6B8E54" stroke-opacity="0.3" stroke-width="2"/>
          </g>

          <!-- Dot 2 — 2025-2026 Vous êtes ici (mimosa pulse) -->
          <g transform="translate(420, 240)" class="ap-svg-g ap-svg-here">
            <circle r="11" fill="#FEF86C"/>
            <circle r="11" fill="#FEF86C" opacity="0.5">
              <animate attributeName="r" from="11" to="26" dur="2s" repeatCount="indefinite"/>
              <animate attributeName="opacity" from="0.5" to="0" dur="2s" repeatCount="indefinite"/>
            </circle>
          </g>

          <!-- Dot 3 — 2027 Standard -->
          <g transform="translate(700, 90)" class="ap-svg-g">
            <circle r="9" fill="#8A8068"/>
          </g>

          <!-- Dot 4 — Retardataires -->
          <g transform="translate(900, 65)" class="ap-svg-g">
            <circle r="9" fill="#8A8068" opacity="0.55"/>
          </g>

          <!-- Labels -->
          <g class="ap-label">
            <text x="160" y="350" text-anchor="middle" class="ap-year-txt">2024</text>
            <text x="160" y="370" text-anchor="middle" class="ap-state-txt">Pionniers</text>
          </g>
          <g class="ap-label ap-label--here">
            <text x="420" y="220" text-anchor="middle" class="ap-year-txt ap-year-here">2025–2026</text>
            <text x="420" y="205" text-anchor="middle" class="ap-state-txt ap-state-here">Différenciation</text>
            <text x="420" y="270" text-anchor="middle" class="ap-here-txt">↑ Vous êtes ici</text>
          </g>
          <g class="ap-label">
            <text x="700" y="55" text-anchor="middle" class="ap-year-txt">2027</text>
            <text x="700" y="40" text-anchor="middle" class="ap-state-txt">Standard</text>
          </g>
          <g class="ap-label">
            <text x="900" y="40" text-anchor="middle" class="ap-year-txt">Après</text>
            <text x="900" y="25" text-anchor="middle" class="ap-state-txt">Retardataires</text>
          </g>

          <!-- Axe X : adoption en % -->
          <text x="60"  y="345" class="ap-axis">2 %</text>
          <text x="960" y="345" text-anchor="end" class="ap-axis">~ 100 %</text>
        </svg>

        <!-- Bénéfices/coûts sous la courbe -->
        <div class="adoption-meta">
          <div class="adoption-cell adoption-cell--pioneer">
            <span class="ac-tag">2024</span>
            <p>Avantage concurrentiel pris.</p>
          </div>
          <div class="adoption-cell adoption-cell--here">
            <span class="ac-tag ac-tag--here">2025–2026 · ici</span>
            <p>Le marché est encore ouvert.</p>
          </div>
          <div class="adoption-cell adoption-cell--std">
            <span class="ac-tag">2027</span>
            <p>Le coût d'entrée devient élevé.</p>
          </div>
          <div class="adoption-cell adoption-cell--late">
            <span class="ac-tag">Après</span>
            <p>Prix de l'attente.</p>
          </div>
        </div>
      </div>

      <p class="reveal adoption-cta">
        <a href="#contact" class="link-arrow">
          Bouger pendant que la courbe monte <Icon name="arrow-right" size={16} class="arrow" />
        </a>
      </p>
    </div>
  </section>

  <!-- =========================================
       SECTION 2 — VOS DONNÉES DORMENT
       ========================================= -->
  <section id="vos-donnees" class="section" aria-labelledby="dorment-title">
    <div class="container">
      <div class="section-head">
        <p class="eyebrow reveal">On vous facilite la tâche</p>
        <h2 id="dorment-title" class="reveal section-h2">
          Développer une entreprise<br>
          n'est pas chose facile.<br>
          <em>9·58 vous facilite la tâche. Plus d'oubli.</em>
        </h2>
        <p class="reveal section-sub">
          Vos données sont déjà là. Vos logiciels ne les regardent pas. Nous, si.
        </p>
      </div>

      <div class="dorment-grid">
        <article class="dorment-item reveal">
          <span class="dorment-n">01</span>
          <h3 class="dorment-h">Ils stockent. Ils n'analysent pas.</h3>
          <p>Vos logiciels métier — quels qu'ils soient — enregistrent vos devis, vos mails, vos heures, vos relevés bancaires. Mais aucun ne croise ces données pour vous dire ce qu'elles signifient pour <em>votre</em> boîte, à <em>votre</em> moment, dans <em>votre</em> secteur.</p>
        </article>

        <article class="dorment-item reveal">
          <span class="dorment-n">02</span>
          <h3 class="dorment-h">Ils décrivent. Ils n'anticipent pas.</h3>
          <p>Votre tableau de bord vous montre votre tréso au 30 mai. 9·58 vous dit : <em>« attention, le client X tarde sur 3 factures, le chantier Y accélère ses appels — vous avez un trou tréso programmé pour le 18 août. Voici les 3 leviers. »</em></p>
        </article>

        <article class="dorment-item reveal">
          <span class="dorment-n">03</span>
          <h3 class="dorment-h">Ils servent tout le monde. Ils ne vous servent pas <em>vous</em>.</h3>
          <p>Un logiciel acheté pense pour 50 000 entreprises à la fois. Forcément, il vous parle à 25 %. Les 75 % restants — vos process, votre secteur, votre clientèle — il n'en sait rien. C'est ce 75 % qu'on récupère.</p>
        </article>
      </div>

      <div class="reveal dorment-chiffre">
        <p class="display-quote">
          « 60 % de votre croissance dort déjà dans vos données. »
        </p>
        <p class="dorment-source">— Constat partagé par les dirigeants qu'on accompagne.</p>
      </div>
    </div>
  </section>

  <!-- =========================================
       SECTION 3 — CE QU'ON CONSTRUIT (3 piliers)
       ========================================= -->
  <section id="construit" class="section section--accent" aria-labelledby="quoi-title">
    <div class="container">
      <p class="eyebrow reveal">Ce qu'on construit</p>
      <h2 id="quoi-title" class="reveal section-h2">
        Un écran. Le vôtre.
      </h2>

      <div class="piliers-grid">
        <div class="pilier reveal">
          <div class="pilier-icon"><Icon name="monitor" size={28} strokeWidth={1.5} /></div>
          <h3 class="pilier-h">L'écran</h3>
          <p class="pilier-sub">Trésorerie, équipes, clients, projets, communication.</p>
          <p class="pilier-body">Toutes vos données métier au même endroit, en temps réel. Vous regardez. Vous savez. Vous décidez. Le matin, en 2 minutes.</p>
        </div>
        <div class="pilier reveal">
          <div class="pilier-icon"><Icon name="sparkles" size={28} strokeWidth={1.5} /></div>
          <h3 class="pilier-h">La couche IA</h3>
          <p class="pilier-sub">Ce qu'aucun logiciel acheté ne sait faire.</p>
          <p class="pilier-body">L'IA croise vos données, anticipe les frictions, rédige vos brouillons, trie vos mails. Sur <em>votre</em> métier, à <em>votre</em> ton, dans <em>votre</em> contexte.</p>
        </div>
        <div class="pilier reveal">
          <div class="pilier-icon"><Icon name="shield" size={28} strokeWidth={1.5} /></div>
          <h3 class="pilier-h">La souveraineté</h3>
          <p class="pilier-sub">À <em>votre</em> nom. En Europe. Pas en location.</p>
          <p class="pilier-body">Mistral, Scaleway, n8n auto-hébergé. Vos données restent à vous, votre outil reste à vous. Rien ne se loue, rien ne triple ses tarifs du jour au lendemain.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- =========================================
       SECTION 4 — LA MÉTHODE
       ========================================= -->
  <section id="methode" class="section" aria-labelledby="methode-title">
    <div class="container is-default">
      <p class="eyebrow reveal">La méthode</p>
      <h2 id="methode-title" class="reveal section-h2">
        Quatre temps. Sans bruit. Sans dépendance.
      </h2>

      <ol class="steps">
        <li class="step reveal">
          <span class="step-n">01</span>
          <div>
            <h3 class="step-h">Audit <span class="step-duree">1 à 2 jours</span></h3>
            <p>Immersion sous NDA. On regarde comment vous travaillez <em>vraiment</em> — pas une moyenne sectorielle. Vous repartez avec un rapport actionnable. Vous pouvez vous arrêter là.</p>
          </div>
        </li>
        <li class="step reveal">
          <span class="step-n">02</span>
          <div>
            <h3 class="step-h">Structuration <span class="step-duree">1 à 3 semaines</span></h3>
            <p>On identifie les trois à cinq points où l'IA change tout dans votre boîte. Plan chiffré, daté, validable.</p>
          </div>
        </li>
        <li class="step reveal">
          <span class="step-n">03</span>
          <div>
            <h3 class="step-h">Construction <span class="step-duree">3 à 12 semaines</span></h3>
            <p>On connecte <em>vos</em> sources, on construit <em>votre</em> écran avec <em>vos</em> indicateurs. Pas de boîte noire. Pas un outil qu'on vous loue.</p>
          </div>
        </li>
        <li class="step reveal">
          <span class="step-n">04</span>
          <div>
            <h3 class="step-h">Mise en route &amp; suivi <span class="step-duree">3 mois à l'infini</span></h3>
            <p>Formation des équipes sur site. Support réactif. Évolutions au rythme de l'IA. Vous devenez autonomes, pas dépendants.</p>
          </div>
        </li>
      </ol>
    </div>
  </section>

  <!-- =========================================
       SECTION 5 — POUR QUI
       ========================================= -->
  <section id="pour-qui" class="section section--soft" aria-labelledby="qui-title">
    <div class="container">
      <p class="eyebrow reveal">Pour qui</p>
      <h2 id="qui-title" class="reveal section-h2">
        Construit pour <em>votre</em> métier,<br>
        pas pour « les entreprises ».
      </h2>

      <div class="cibles-grid">
        {cibles.map((c) => (
          <article class="cible reveal">
            <div class="cible-icon"><Icon name={c.icon} size={22} strokeWidth={1.6} /></div>
            <div>
              <h3 class="cible-h">{c.metier}</h3>
              <p class="cible-p">{c.pitch}</p>
            </div>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- =========================================
       SECTION 6 — PREUVE
       ========================================= -->
  <section id="preuve" class="section" aria-labelledby="preuve-title">
    <div class="container">
      <p class="eyebrow reveal">Ce qu'on fait depuis 2022</p>
      <h2 id="preuve-title" class="reveal section-h2">
        Pas une démo dingue. Le quotidien qui change.
      </h2>

      <div class="proofs-grid">
        {proofs.map((p) => (
          <div class="proof reveal">
            <span class="proof-v">{p.value}</span>
            <span class="proof-l">{p.label}</span>
          </div>
        ))}
      </div>

      <hr class="rule" style="margin-block: var(--s-9);" />

      <div class="temoignages-grid">
        {temoignages.map((t) => (
          <figure class="temoignage reveal">
            <span class="chip">{t.secteur}</span>
            <blockquote>« {t.quote} »</blockquote>
            <figcaption>{t.result}</figcaption>
          </figure>
        ))}
      </div>
    </div>
  </section>

  <!-- =========================================
       SECTION 7 — CTA FINAL
       ========================================= -->
  <section id="contact" class="section section--accent" aria-labelledby="cta-title">
    <div class="container is-default cta-inner">
      <p class="eyebrow reveal">Commencer</p>
      <h2 id="cta-title" class="reveal cta-title">
        Un audit. Un à deux jours. <em>Sous accord de confidentialité.</em>
      </h2>
      <p class="reveal cta-lead">
        Je regarde votre contexte. Je vous dis honnêtement ce que 9·58 peut faire pour vous —
        et ce qu'il ne peut pas. Sans engagement pour la suite.
      </p>
      <div class="cta-ctas reveal">
        <a href="mailto:jmg@958.fr?subject=Demande%20de%20diagnostic%209%C2%B758" class="btn btn--lg btn-on-accent">
          Demander un diagnostic
          <Icon name="arrow-right" size={18} class="arrow" />
        </a>
        <a href="https://demo.958.fr" class="btn btn--ghost btn--lg btn--on-accent-ghost">
          Voir une démo
          <Icon name="arrow-up-right" size={18} class="arrow" />
        </a>
      </div>
    </div>
  </section>

  <!-- =========================================
       FOOTER mini
       ========================================= -->
  <footer class="footer-mini section--dark" role="contentinfo">
    <div class="container footer-mini-inner">
      <div>
        <span class="brand-mark">9·58</span>
        <span class="brand-tag">Neuf Cinquante Huit · Vannes &amp; Paris</span>
      </div>
      <ul class="footer-meta">
        <li><a href="mailto:jmg@958.fr">jmg@958.fr</a></li>
        <li><a href="https://www.linkedin.com/in/jean-marc-geuring/" rel="me noopener" target="_blank">LinkedIn</a></li>
        <li><a href="/mentions-legales/">Mentions légales</a></li>
      </ul>
      <span class="footer-year">© 2026</span>
    </div>
  </footer>
</V2bLayout>

<style>
  /* ============ HEADER ============ */
  .site-header {
    position: sticky;
    top: 0;
    z-index: 50;
    background: rgba(11, 11, 10, 0.78);
    backdrop-filter: saturate(180%) blur(12px);
    -webkit-backdrop-filter: saturate(180%) blur(12px);
    border-bottom: 1px solid var(--line);
  }
  .header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: var(--header-h);
    gap: var(--s-5);
  }
  .brand {
    display: inline-flex;
    flex-direction: column;
    line-height: 1;
    color: var(--ivoire);
    text-decoration: none;
    gap: 3px;
  }
  .brand-mark {
    font-family: var(--font-display);
    font-weight: 500;
    font-size: 1.5rem;
    letter-spacing: -0.02em;
    color: var(--cuivre);
  }
  .brand-tag {
    font-family: var(--font-mono);
    font-size: 10px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--ink-3);
  }
  .nav-desktop ul {
    display: flex;
    gap: var(--s-5);
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .nav-desktop a {
    font-size: var(--step-0);
    font-weight: 500;
    color: var(--ink-2);
    padding: 6px 2px;
    border-bottom: 1px solid transparent;
    transition: color var(--dur-1) var(--ease),
                border-color var(--dur-1) var(--ease);
  }
  .nav-desktop a:hover {
    color: var(--cuivre);
    border-bottom-color: var(--cuivre);
  }

  @media (max-width: 900px) {
    .nav-desktop, .header-cta { display: none; }
  }

  /* ============ HERO (layout 2 col + démo) ============ */
  .hero {
    padding-block: var(--s-10) var(--s-9);
  }
  .hero-grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1.15fr);
    gap: var(--s-9);
    align-items: center;
  }
  .hero-copy {
    display: flex;
    flex-direction: column;
  }
  .hero-copy .eyebrow { margin-bottom: var(--s-5); }
  .hero-copy .manifesto {
    margin-bottom: var(--s-5);
    max-width: 14ch;
  }
  .hero-copy .killer-line {
    margin-bottom: var(--s-6);
  }
  .hero-ctas {
    display: flex;
    gap: var(--s-3);
    flex-wrap: wrap;
    align-items: center;
  }
  .hero-assurances {
    display: flex;
    flex-wrap: wrap;
    gap: var(--s-5);
    margin-top: var(--s-6);
    list-style: none;
    padding: 0;
  }
  .hero-assurances li {
    font-family: var(--font-mono);
    font-size: var(--step--2);
    color: var(--ink-3);
    letter-spacing: 0.04em;
    text-transform: uppercase;
    position: relative;
    padding-left: 14px;
  }
  .hero-assurances li::before {
    content: "";
    position: absolute;
    left: 0;
    top: 50%;
    transform: translateY(-50%);
    width: 6px;
    height: 6px;
    background: var(--cuivre);
    border-radius: 50%;
  }

  @media (max-width: 1024px) {
    .hero-grid {
      grid-template-columns: 1fr;
      gap: var(--s-8);
    }
    .hero-copy .manifesto { max-width: 100%; }
  }

  @media (max-width: 720px) {
    .hero { padding-block: var(--s-7) var(--s-7); }
  }

  /* ============ COCKPIT DEMO (mockup) ============ */
  .hero-demo {
    perspective: 1200px;
  }
  .cockpit {
    background: var(--bg-2);
    border: 1px solid var(--line-2);
    border-radius: 16px;
    overflow: hidden;
    box-shadow:
      0 30px 80px -20px rgba(0,0,0,0.6),
      0 0 0 1px rgba(244, 236, 220, 0.05),
      0 8px 24px -8px rgba(199, 95, 45, 0.18);
    transform: rotateY(-2deg) rotateX(2deg);
    transition: transform 0.6s var(--ease);
  }
  .cockpit:hover {
    transform: rotateY(0) rotateX(0);
  }

  .cockpit-bar {
    display: flex;
    align-items: center;
    gap: var(--s-4);
    padding: 12px 16px;
    background: var(--bg);
    border-bottom: 1px solid var(--line);
    font-family: var(--font-mono);
    font-size: 12px;
    letter-spacing: 0.04em;
  }
  .cockpit-brand {
    font-family: var(--font-display);
    font-weight: 500;
    color: var(--cuivre);
    font-size: 16px;
    letter-spacing: -0.02em;
  }
  .cockpit-cabinet {
    color: var(--ink-2);
    flex: 1;
    text-transform: uppercase;
  }
  .cockpit-time {
    color: var(--ink-3);
  }
  .cockpit-version {
    font-size: 10px;
    color: var(--cuivre);
    border: 1px solid var(--cuivre);
    padding: 2px 8px;
    border-radius: 999px;
    background: var(--cuivre-soft);
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .cockpit-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1px;
    background: var(--line);
  }
  .cockpit-card {
    background: var(--bg-2);
    padding: var(--s-4) var(--s-5);
    display: flex;
    flex-direction: column;
    gap: var(--s-3);
    min-height: 130px;
  }
  .cockpit-card--ai {
    grid-column: 1 / -1;
    background: linear-gradient(180deg, var(--cuivre-soft) 0%, var(--bg-2) 100%);
    border-top: 1px solid var(--cuivre);
  }
  .cc-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }
  .cc-label {
    font-family: var(--font-mono);
    font-size: 10px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--ink-3);
  }
  .cc-label--ai {
    color: var(--cuivre);
    display: inline-flex;
    align-items: center;
    gap: 8px;
  }
  .cc-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--vert-mousse);
  }
  .cc-dot--warn { background: var(--warn); }
  .cc-pulse {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--cuivre);
    box-shadow: 0 0 0 0 var(--cuivre);
    animation: cc-pulse 2s var(--ease) infinite;
  }
  @keyframes cc-pulse {
    0%, 100% { box-shadow: 0 0 0 0 rgba(199, 95, 45, 0.6); }
    50% { box-shadow: 0 0 0 8px transparent; }
  }

  .cc-value {
    font-family: var(--font-display);
    font-weight: 500;
    font-size: 28px;
    line-height: 1;
    letter-spacing: -0.03em;
    color: var(--ivoire);
  }
  .cc-spark {
    color: var(--cuivre);
    margin-top: -4px;
  }
  .cc-spark svg {
    width: 100%;
    height: 24px;
  }
  .cc-note {
    font-family: var(--font-mono);
    font-size: 10px;
    color: var(--warn);
    margin: 0;
    max-width: none;
    letter-spacing: 0.02em;
  }

  .cc-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 6px;
  }
  .cc-list li {
    font-size: 12px;
    color: var(--ink-2);
    display: flex;
    align-items: center;
    gap: 8px;
    line-height: 1.4;
  }

  /* Chantiers — progress rows */
  .cc-chantiers {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .cc-chantiers li {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }
  .cc-chantier-row {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 11px;
    color: var(--ink-2);
  }
  .cc-chantier-nom { flex: 1; }
  .cc-chantier-pct {
    font-family: var(--font-mono);
    font-size: 10px;
    color: var(--ink-3);
    letter-spacing: 0.02em;
  }
  .cc-progress {
    height: 4px;
    background: var(--bg);
    border-radius: 999px;
    overflow: hidden;
    position: relative;
  }
  .cc-progress > span {
    display: block;
    height: 100%;
    border-radius: 999px;
    transition: width 0.8s var(--ease-out);
  }

  /* RH / Équipe */
  .cc-rh {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .cc-rh li {
    display: grid;
    grid-template-columns: 60px 1fr 32px;
    align-items: center;
    gap: 8px;
    font-size: 11px;
    color: var(--ink-2);
  }
  .cc-rh-name {
    font-weight: 500;
    color: var(--ivoire-2);
  }
  .cc-rh-pct {
    font-family: var(--font-mono);
    font-size: 10px;
    color: var(--ink-3);
    text-align: right;
    letter-spacing: 0.02em;
  }
  .cc-note--info {
    color: var(--ink-3);
    margin-top: 4px;
  }

  /* Pipeline kanban */
  .cockpit-card--kanban {
    grid-column: 1 / -1;
    padding: var(--s-4) var(--s-5);
  }
  .cc-kanban-meta {
    font-family: var(--font-mono);
    font-size: 9px;
    color: var(--ink-3);
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }
  .cc-kanban {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 8px;
    margin-top: 4px;
  }
  .cc-kcol {
    background: var(--bg);
    border: 1px solid var(--line);
    border-radius: 6px;
    padding: 8px;
    display: flex;
    flex-direction: column;
    gap: 6px;
    position: relative;
    min-height: 110px;
  }
  .cc-kcol-h {
    font-family: var(--font-mono);
    font-size: 9px;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--cuivre);
    font-weight: 600;
  }
  .cc-kcard {
    background: var(--bg-3);
    border: 1px solid var(--line-2);
    border-radius: 4px;
    padding: 5px 7px;
    font-size: 10px;
    color: var(--ivoire-2);
    line-height: 1.3;
    transition: transform 0.6s var(--ease), background 0.6s var(--ease);
  }
  .cc-kcard--moving {
    animation: kanban-move 6s var(--ease) infinite;
    border-color: var(--cuivre);
    box-shadow: 0 0 0 1px var(--cuivre-soft);
  }
  @keyframes kanban-move {
    0%, 25% { transform: translate(0, 0); background: var(--bg-3); }
    35%, 40% { transform: translate(0, 60px); background: var(--cuivre-soft); opacity: 0.7; }
    50%, 75% { transform: translate(calc(100% + 12px), 60px); background: var(--cuivre-soft); opacity: 1; }
    85%, 90% { transform: translate(calc(100% + 12px), 0); background: var(--cuivre-soft); }
    100% { transform: translate(0, 0); background: var(--bg-3); opacity: 1; }
  }
  .cc-kcol-count {
    position: absolute;
    top: 6px;
    right: 8px;
    font-family: var(--font-mono);
    font-size: 9px;
    color: var(--ink-3);
    background: var(--bg-3);
    padding: 1px 5px;
    border-radius: 999px;
  }
  .cc-tag {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 16px;
    height: 16px;
    border-radius: 4px;
    font-size: 10px;
    font-weight: 700;
    flex-shrink: 0;
    background: var(--bg-3);
    color: var(--ink-3);
  }
  .cc-tag--ok { background: var(--vert-mousse-soft); color: var(--vert-mousse); }
  .cc-tag--warn { background: var(--warn-soft); color: var(--warn); }

  .cc-mail-draft {
    background: var(--bg);
    border: 1px solid var(--line);
    border-radius: 8px;
    padding: 10px 12px;
    display: flex;
    flex-direction: column;
    gap: 4px;
    position: relative;
  }
  .cc-mail-to {
    font-family: var(--font-mono);
    font-size: 10px;
    color: var(--ink-3);
    letter-spacing: 0.04em;
  }
  .cc-mail-body {
    margin: 0;
    max-width: none;
    font-size: 12px;
    color: var(--ivoire-2);
    line-height: 1.4;
  }
  .typing {
    display: inline-block;
    overflow: hidden;
    white-space: nowrap;
    border-right: 1.5px solid var(--cuivre);
    animation: typing 4s steps(28) infinite, blink 1s step-end infinite;
    width: 28ch;
    max-width: 100%;
  }
  @keyframes typing {
    0% { width: 0; }
    60% { width: 28ch; }
    90% { width: 28ch; }
    100% { width: 0; }
  }
  @keyframes blink {
    50% { border-color: transparent; }
  }
  .cc-mail-badge {
    position: absolute;
    top: -8px;
    right: 8px;
    background: var(--cuivre);
    color: var(--bg);
    font-family: var(--font-mono);
    font-size: 9px;
    font-weight: 600;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    padding: 2px 8px;
    border-radius: 999px;
  }

  .cc-ai-text {
    margin: 0;
    max-width: none;
    font-size: 13px;
    color: var(--ivoire);
    line-height: 1.5;
  }
  .cc-ai-text strong {
    color: var(--cuivre);
    font-weight: 600;
  }
  .cc-ai-actions {
    display: flex;
    gap: 8px;
    margin-top: 4px;
  }
  .cc-ai-btn {
    font-family: var(--font-sans);
    font-size: 11px;
    font-weight: 600;
    padding: 6px 12px;
    border-radius: 6px;
    border: 1px solid var(--line-2);
    background: transparent;
    color: var(--ink-2);
    cursor: pointer;
    transition: all var(--dur-1) var(--ease);
  }
  .cc-ai-btn:hover {
    border-color: var(--cuivre);
    color: var(--cuivre);
  }
  .cc-ai-btn--primary {
    background: var(--cuivre);
    color: var(--bg);
    border-color: var(--cuivre);
  }
  .cc-ai-btn--primary:hover {
    background: var(--cuivre-hover);
    color: var(--bg);
  }

  @media (max-width: 720px) {
    .cockpit { transform: none; }
    .cockpit-grid { grid-template-columns: 1fr; }
    .cockpit-card { min-height: auto; }
    .cc-value { font-size: 24px; }
  }

  /* ============ SECTION HEAD ============ */
  .section-head {
    display: flex;
    flex-direction: column;
    gap: var(--s-3);
    margin-bottom: var(--s-8);
    max-width: 720px;
  }
  .section-h2 {
    font-family: var(--font-display);
    font-size: clamp(2rem, 4vw + 0.5rem, 3.25rem);
    font-weight: 400;
    line-height: 1.05;
    letter-spacing: var(--tr-tighter);
    color: var(--ink);
    max-width: 22ch;
    text-wrap: balance;
    margin: 0;
  }
  .section-h2 em { font-style: italic; color: var(--cuivre); }
  .section-sub {
    font-size: var(--step-2);
    line-height: var(--lh-normal);
    color: var(--ink-2);
    max-width: 60ch;
    margin: 0;
  }

  /* ============ BANDEAU CLARIF (anti "ça fait CRM") ============ */
  .section--clarif {
    background: var(--bg);
    border-top: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
    padding-block: var(--s-7);
  }
  .clarif-text {
    font-family: var(--font-display);
    font-size: clamp(1.125rem, 1.5vw + 0.3rem, 1.5rem);
    line-height: 1.5;
    color: var(--ivoire-2);
    max-width: 760px;
    margin: 0 auto;
    text-align: center;
    text-wrap: balance;
    font-weight: 400;
  }
  .clarif-text strong { color: var(--ivoire); font-weight: 500; }
  .clarif-text em { color: var(--cuivre); font-style: italic; font-weight: 500; }

  /* ============ COURBE D'ADOPTION (SVG) ============ */
  .adoption-curve {
    margin-top: var(--s-7);
    color: var(--ivoire);
  }
  .adoption-svg {
    width: 100%;
    height: auto;
    max-height: 440px;
    display: block;
  }
  .adoption-path {
    stroke-dasharray: 1500;
    stroke-dashoffset: 1500;
    animation: drawCurve 2.4s var(--ease-out) 0.3s forwards;
  }
  @keyframes drawCurve {
    to { stroke-dashoffset: 0; }
  }

  .ap-year-txt {
    font-family: var(--font-mono);
    font-size: 11px;
    fill: var(--ink-3);
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }
  .ap-state-txt {
    font-family: var(--font-display);
    font-size: 14px;
    fill: var(--ivoire);
    font-weight: 500;
  }
  .ap-year-here {
    fill: var(--mimosa);
    font-weight: 600;
  }
  .ap-state-here {
    fill: var(--mimosa);
    font-style: italic;
  }
  .ap-here-txt {
    font-family: var(--font-mono);
    font-size: 11px;
    fill: var(--mimosa);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    font-weight: 600;
  }
  .ap-axis {
    font-family: var(--font-mono);
    font-size: 10px;
    fill: var(--ink-3);
    letter-spacing: 0.04em;
  }

  /* Tableau métadonnées sous la courbe */
  .adoption-meta {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--s-3);
    margin-top: var(--s-5);
  }
  .adoption-cell {
    padding: var(--s-4);
    background: var(--bg);
    border: 1px solid var(--line);
    border-radius: var(--r-3);
    display: flex;
    flex-direction: column;
    gap: var(--s-2);
  }
  .adoption-cell--here {
    background: rgba(254, 248, 108, 0.06);
    border-color: var(--mimosa);
  }
  .adoption-cell--pioneer .ac-tag { color: var(--vert-mousse); border-color: var(--vert-mousse); }
  .adoption-cell--std .ac-tag { color: var(--ink-3); }
  .adoption-cell--late .ac-tag { color: var(--ink-3); opacity: 0.7; }
  .ac-tag {
    display: inline-block;
    font-family: var(--font-mono);
    font-size: 10px;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--cuivre);
    padding: 3px 8px;
    border: 1px solid currentColor;
    border-radius: var(--r-pill);
    align-self: flex-start;
    font-weight: 500;
  }
  .ac-tag--here {
    color: var(--mimosa);
    background: var(--mimosa);
    color: var(--bg);
    border-color: var(--mimosa);
  }
  .adoption-cell p {
    font-size: var(--step-0);
    color: var(--ink-2);
    margin: 0;
    line-height: var(--lh-normal);
    max-width: none;
  }

  @media (max-width: 720px) {
    .adoption-meta { grid-template-columns: repeat(2, 1fr); }
  }

  /* ============ SCHÉMA ADOPTION (legacy, conservé) ============ */
  .adoption-schema {
    position: relative;
    margin-top: var(--s-7);
    padding-block: var(--s-7) var(--s-5);
  }
  .adoption-line {
    position: absolute;
    top: 90px;
    left: 6%;
    right: 6%;
    height: 2px;
    background: linear-gradient(90deg,
      var(--cuivre) 0%,
      var(--cuivre) 30%,
      var(--mimosa) 38%,
      var(--mimosa) 52%,
      var(--ink-3) 70%,
      var(--ink-3) 100%);
    opacity: 0.55;
  }
  .adoption-points {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--s-4);
    position: relative;
  }
  .adoption-point {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: var(--s-2);
    padding-top: 50px;
    position: relative;
  }
  .ap-dot {
    width: 18px;
    height: 18px;
    border-radius: 50%;
    background: var(--cuivre);
    position: absolute;
    top: 82px;
    left: 50%;
    transform: translateX(-50%);
    box-shadow: 0 0 0 4px var(--bg-2);
  }
  .ap-pioneer .ap-dot { background: var(--vert-mousse); }
  .ap-now .ap-dot-pulse {
    background: var(--mimosa);
    width: 22px;
    height: 22px;
    top: 80px;
    animation: ap-pulse 2.4s var(--ease) infinite;
  }
  .ap-standard .ap-dot { background: var(--ink-3); }
  .ap-late .ap-dot { background: var(--ink-3); opacity: 0.55; }

  @keyframes ap-pulse {
    0%, 100% { box-shadow: 0 0 0 4px var(--bg-2), 0 0 0 0 var(--mimosa-soft, rgba(254,248,108,0.45)); }
    50%      { box-shadow: 0 0 0 4px var(--bg-2), 0 0 0 12px transparent; }
  }

  .ap-here {
    position: absolute;
    top: 24px;
    left: 50%;
    transform: translateX(-50%);
    font-family: var(--font-mono);
    font-size: var(--step--2);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--mimosa);
    white-space: nowrap;
    padding: 4px 10px;
    background: var(--bg);
    border: 1px solid var(--mimosa);
    border-radius: var(--r-pill);
    font-weight: 500;
  }
  .ap-here::after {
    content: "";
    position: absolute;
    left: 50%;
    top: 100%;
    transform: translateX(-50%);
    border: 5px solid transparent;
    border-top-color: var(--mimosa);
    margin-top: 1px;
  }

  .ap-year {
    font-family: var(--font-mono);
    font-size: var(--step--1);
    letter-spacing: 0.06em;
    color: var(--ink-3);
    text-transform: uppercase;
    margin-top: var(--s-4);
  }
  .ap-now .ap-year { color: var(--mimosa); }

  .ap-state {
    font-family: var(--font-display);
    font-weight: 500;
    font-size: var(--step-2);
    color: var(--ink);
    letter-spacing: -0.015em;
    margin: 0;
    line-height: 1.2;
    max-width: 18ch;
  }
  .ap-pioneer .ap-state { color: var(--ivoire); }
  .ap-now .ap-state { color: var(--ivoire); }
  .ap-late .ap-state { color: var(--ink-2); }

  .ap-desc {
    font-size: var(--step-0);
    color: var(--ink-2);
    margin: 0;
    max-width: 22ch;
    line-height: var(--lh-normal);
  }
  .ap-pioneer .ap-desc { color: var(--vert-mousse); }
  .ap-now .ap-desc { color: var(--mimosa); }
  .ap-late .ap-desc { color: var(--ink-3); }

  .adoption-cta {
    margin-top: var(--s-6);
  }

  @media (max-width: 720px) {
    .adoption-points { grid-template-columns: 1fr; gap: var(--s-6); }
    .adoption-line {
      top: 0;
      bottom: 0;
      left: 30px;
      right: auto;
      width: 2px;
      height: auto;
      background: linear-gradient(180deg,
        var(--vert-mousse) 0%,
        var(--cuivre) 20%,
        var(--mimosa) 35%,
        var(--mimosa) 52%,
        var(--ink-3) 70%,
        var(--ink-3) 100%);
    }
    .adoption-point {
      align-items: flex-start;
      text-align: left;
      padding-top: 0;
      padding-left: 60px;
      min-height: 80px;
    }
    .ap-dot {
      top: 8px;
      left: 30px;
      transform: translateX(-50%);
    }
    .ap-now .ap-dot-pulse { top: 6px; }
    .ap-here {
      position: static;
      transform: none;
      margin-bottom: var(--s-2);
      display: inline-block;
    }
    .ap-here::after { display: none; }
    .ap-year { margin-top: 0; }
    .ap-state, .ap-desc { max-width: 100%; }
  }

  /* ============ DORMENT (Vos données dorment) ============ */
  .dorment-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--s-6);
    margin-top: var(--s-5);
  }
  .dorment-item {
    display: flex;
    flex-direction: column;
    gap: var(--s-3);
  }
  .dorment-n {
    font-family: var(--font-display);
    font-weight: 400;
    font-size: clamp(2rem, 3vw + 0.5rem, 2.75rem);
    line-height: 1;
    color: var(--cuivre);
    letter-spacing: -0.04em;
  }
  .dorment-h {
    font-family: var(--font-display);
    font-weight: 500;
    font-size: var(--step-3);
    color: var(--ink);
    letter-spacing: -0.02em;
    margin: 0;
    line-height: 1.2;
  }
  .dorment-h em { color: var(--cuivre); font-style: italic; }
  .dorment-item p { margin: 0; max-width: none; font-size: var(--step-1); }

  .dorment-chiffre {
    margin-top: var(--s-10);
    padding-top: var(--s-8);
    border-top: 1px solid var(--line);
    display: flex;
    flex-direction: column;
    gap: var(--s-3);
  }
  .dorment-source {
    font-family: var(--font-mono);
    font-size: var(--step--2);
    color: var(--ink-3);
    letter-spacing: 0.04em;
    text-transform: uppercase;
    margin: 0;
    max-width: none;
  }

  @media (max-width: 900px) {
    .dorment-grid { grid-template-columns: 1fr; gap: var(--s-7); }
  }

  /* ============ STEPS (méthode) ============ */
  .steps {
    list-style: none;
    padding: 0;
    margin: var(--s-7) 0 0 0;
    display: flex;
    flex-direction: column;
    gap: var(--s-5);
  }
  .step {
    display: grid;
    grid-template-columns: 72px 1fr;
    gap: var(--s-5);
    padding: var(--s-5);
    background: var(--bg-2);
    border: 1px solid var(--line);
    border-radius: var(--r-3);
    transition: border-color var(--dur-2) var(--ease);
  }
  .step:hover { border-color: var(--cuivre); }
  .step-n {
    font-family: var(--font-mono);
    font-size: var(--step-1);
    color: var(--cuivre);
    letter-spacing: 0.04em;
    font-weight: 500;
  }
  .step-h {
    font-family: var(--font-display);
    font-weight: 500;
    font-size: var(--step-3);
    color: var(--ink);
    margin: 0 0 var(--s-2) 0;
    letter-spacing: -0.02em;
    display: flex;
    flex-wrap: wrap;
    gap: var(--s-3);
    align-items: baseline;
  }
  .step-duree {
    font-family: var(--font-mono);
    font-size: var(--step--2);
    color: var(--ink-3);
    letter-spacing: 0.06em;
    text-transform: uppercase;
    font-weight: 400;
  }
  .step p { margin: 0; max-width: 64ch; }

  @media (max-width: 640px) {
    .step { grid-template-columns: 1fr; gap: var(--s-3); padding: var(--s-4); }
  }

  /* ============ PILIERS (accent cuivre) ============ */
  .piliers-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--s-5);
    margin-top: var(--s-7);
  }
  .pilier {
    padding: var(--s-6);
    background: rgba(11, 11, 10, 0.28);
    border: 1px solid rgba(244, 236, 220, 0.2);
    border-radius: var(--r-3);
    display: flex;
    flex-direction: column;
    gap: var(--s-3);
  }
  .pilier-icon {
    width: 48px;
    height: 48px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: var(--bg);
    color: var(--cuivre);
    border-radius: var(--r-3);
    margin-bottom: var(--s-2);
  }
  .pilier-h {
    font-family: var(--font-display);
    font-weight: 500;
    font-size: var(--step-3);
    color: var(--ivoire);
    letter-spacing: -0.02em;
    margin: 0;
  }
  .pilier-sub {
    font-family: var(--font-display);
    font-style: italic;
    color: var(--ivoire);
    margin: 0;
    max-width: none;
    font-size: var(--step-1);
  }
  .pilier-sub em { color: var(--ivoire); font-style: normal; font-weight: 600; }
  .pilier-body {
    color: var(--ivoire-2);
    font-size: var(--step-1);
    line-height: var(--lh-normal);
    margin: 0;
    max-width: none;
  }
  .pilier-body em { color: var(--ivoire); font-style: italic; }

  @media (max-width: 900px) {
    .piliers-grid { grid-template-columns: 1fr; }
  }

  /* ============ CIBLES (Pour qui) ============ */
  .cibles-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--s-4);
    margin-top: var(--s-7);
  }
  .cible {
    display: flex;
    gap: var(--s-4);
    padding: var(--s-5);
    background: var(--bg);
    border: 1px solid var(--line);
    border-radius: var(--r-3);
    align-items: flex-start;
    transition: border-color var(--dur-2) var(--ease),
                transform var(--dur-2) var(--ease);
  }
  .cible:hover {
    border-color: var(--cuivre);
    transform: translateY(-2px);
  }
  .cible-icon {
    width: 40px;
    height: 40px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: var(--cuivre-soft);
    color: var(--cuivre);
    border-radius: var(--r-2);
    flex-shrink: 0;
  }
  .cible-h {
    font-family: var(--font-display);
    font-weight: 500;
    font-size: var(--step-2);
    color: var(--ink);
    margin: 0 0 var(--s-1) 0;
    letter-spacing: -0.015em;
    line-height: 1.2;
  }
  .cible-p {
    font-size: var(--step-0);
    color: var(--ink-2);
    line-height: var(--lh-normal);
    margin: 0;
    max-width: none;
  }

  @media (max-width: 900px) { .cibles-grid { grid-template-columns: 1fr; } }

  /* ============ PROOFS ============ */
  .proofs-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--s-6);
    margin-top: var(--s-7);
  }
  .proof {
    display: flex;
    flex-direction: column;
    gap: var(--s-2);
  }
  .proof-v {
    font-family: var(--font-display);
    font-size: clamp(2.5rem, 5vw + 0.5rem, 4.25rem);
    font-weight: 500;
    line-height: 1;
    letter-spacing: -0.04em;
    color: var(--cuivre);
  }
  .proof-l {
    font-size: var(--step-0);
    color: var(--ink-2);
    line-height: var(--lh-normal);
    max-width: 22ch;
  }

  @media (max-width: 720px) {
    .proofs-grid { grid-template-columns: repeat(2, 1fr); gap: var(--s-5); }
  }

  /* ============ TÉMOIGNAGES ============ */
  .temoignages-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--s-5);
  }
  .temoignage {
    display: flex;
    flex-direction: column;
    gap: var(--s-4);
    padding: var(--s-5);
    background: var(--bg-2);
    border: 1px solid var(--line);
    border-radius: var(--r-3);
    margin: 0;
  }
  .temoignage blockquote {
    font-family: var(--font-display);
    font-style: italic;
    font-size: var(--step-2);
    line-height: 1.4;
    color: var(--ink);
    font-weight: 400;
    margin: 0;
    max-width: none;
  }
  .temoignage figcaption {
    font-family: var(--font-mono);
    font-size: var(--step--2);
    color: var(--cuivre);
    font-weight: 500;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  @media (max-width: 900px) { .temoignages-grid { grid-template-columns: 1fr; } }

  /* ============ CTA FINAL ============ */
  .cta-inner {
    display: flex;
    flex-direction: column;
    gap: var(--s-5);
    align-items: flex-start;
  }
  .cta-title {
    font-family: var(--font-display);
    font-size: clamp(2rem, 4vw + 1rem, 3.5rem);
    font-weight: 400;
    line-height: 1.05;
    letter-spacing: var(--tr-tighter);
    color: var(--ivoire);
    max-width: 22ch;
    text-wrap: balance;
    margin: 0;
  }
  .cta-title em { font-style: italic; }
  .cta-lead {
    font-size: var(--step-2);
    line-height: var(--lh-normal);
    color: var(--ivoire-2);
    max-width: 56ch;
    margin: 0;
  }
  .cta-ctas {
    display: flex;
    gap: var(--s-3);
    flex-wrap: wrap;
    margin-top: var(--s-3);
  }
  .btn-on-accent {
    background: var(--bg);
    color: var(--ivoire);
    border: 1px solid var(--bg);
  }
  .btn-on-accent:hover {
    background: var(--ivoire);
    color: var(--bg);
    border-color: var(--ivoire);
    transform: translateY(-1px);
    box-shadow: var(--shadow-lg);
  }
  .btn--on-accent-ghost {
    color: var(--ivoire);
    border-color: var(--ivoire);
  }
  .btn--on-accent-ghost:hover {
    background: var(--ivoire);
    color: var(--cuivre);
    border-color: var(--ivoire);
  }

  /* ============ FOOTER ============ */
  .footer-mini {
    padding-block: var(--s-6);
    border-top: 1px solid var(--line);
    background: var(--bg-2);
  }
  .footer-mini-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--s-5);
    flex-wrap: wrap;
  }
  .footer-mini-inner > div:first-child {
    display: inline-flex;
    flex-direction: column;
    gap: 3px;
    line-height: 1;
  }
  .footer-meta {
    display: flex;
    gap: var(--s-5);
    list-style: none;
    padding: 0;
    margin: 0;
    font-family: var(--font-mono);
    font-size: var(--step--2);
    letter-spacing: 0.04em;
  }
  .footer-meta a {
    color: var(--ink-2);
    text-transform: uppercase;
  }
  .footer-meta a:hover { color: var(--cuivre); }
  .footer-year {
    font-family: var(--font-mono);
    font-size: var(--step--2);
    color: var(--ink-3);
    letter-spacing: 0.06em;
  }
</style>
